import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 20,
    },
    image: {
        height: 250,
        width: 150,
        resizeMode: 'cover',
        borderRadius: 5,
    }
})

export default styles;