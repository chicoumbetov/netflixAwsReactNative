import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        padding: 20,
        marginTop: 20,
    },
    title: {
        fontSize: 16,
        color: 'white',
    },
    image: {
        height: 150,
        width: 100,
        resizeMode: 'cover',
        borderRadius: 5,
        margin: 5,
    }
})

export default styles;